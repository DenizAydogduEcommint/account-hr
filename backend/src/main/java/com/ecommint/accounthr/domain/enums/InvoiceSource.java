package com.ecommint.accounthr.domain.enums;

/** Faturanın geldiği kaynak: Servis paneli / E-posta / e-Fatura / Drive waiting. */
public enum InvoiceSource {
    SERVICE_PANEL,
    EMAIL,
    E_INVOICE,
    DRIVE_WAITING
}
